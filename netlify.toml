# Netlify build-configuratie voor Ellen Bruist Mee (Vite + React)
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Vite 8 vereist Node 20.19+ / 22.12+
  NODE_VERSION = "22"
